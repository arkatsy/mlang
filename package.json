{
  "name": "mini-programming-language",
  "scripts": {
    "build": "rm -rf ./build && tsc -p ./tsconfig.json && node build/index.js",
    "only-build": "rm -rf ./build && tsc -p ./tsconfig.json",
    "test": "pnpm build && jest"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^18.15.11",
    "@types/readline-sync": "^1.4.4",
    "jest": "^29.5.0",
    "pkg": "^5.8.1",
    "ts-jest": "^29.0.5",
    "ts-node": "^10.9.1",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^5.0.2"
  },
  "dependencies": {
    "readline-sync": "^1.4.10"
  }
}
